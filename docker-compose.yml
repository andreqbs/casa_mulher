services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8086:80"
    restart: unless-stopped
    environment:
      FLORA_API_TOKEN: ${FLORA_API_TOKEN}
      CHATKIT_OPENAI_API_KEY: ${CHATKIT_OPENAI_API_KEY}
